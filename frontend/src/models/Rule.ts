/**
 * Interface for rule.
 *
 * @param type - Rule type
 * @param description - Rule description
 * @param value - Rule value
 */
export interface Rule {
    type: string,
    description: string,
    value: number
}