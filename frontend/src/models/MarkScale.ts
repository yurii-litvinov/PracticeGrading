/**
 * Interface for mark scale.
 *
 * @param id - Mark scale id
 * @param min - Mark scale minimum
 * @param max - Mark scale maximum
 * @param mark - Mark scale mark
 */
export interface MarkScale {
    id?: number,
    min?: number,
    max?: number,
    mark?: string
}